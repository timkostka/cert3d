#pragma once

#include "c3d_includes.h"

// A C3D_Thermistor object simulates a thermistor.

/*
struct C3D_Thermistor {
  // DAC to use
  DAC_TypeDef * DACx;
  // nominal resistance value in ohms
  float nominal_resistance;

  // constructor
  C3D_Thermistor() {
    // enable DAC clock
    HAL_DAC_SetValue();
  }

  // set a value
  void SetRawValue(uint16_t value) {
    GSL_DAC_GenerateSignal();
  }

};
*/
